name: deeplink_connect
description: "Plugin construido para auxiliar consumo de pagamentos por deeplink"
version: 0.0.1
homepage:

environment:
  sdk: '>=3.4.1 <4.0.0'
  flutter: '>=3.3.0'

dependencies:
  flutter:
    sdk: flutter
  plugin_platform_interface: ^2.0.2

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter:

  plugin:
    platforms:
      android:
        package: br.com.isrtec.deeplink_connect
        pluginClass: DeeplinkConnectPlugin

